# ---------------------------------------------------------------------------
# Banco de dados
# ---------------------------------------------------------------------------
# ATENCAO: no Railway o valor NAO e uma URL. E a referencia literal abaixo,
# digitada com as chaves duplas - o Railway resolve no deploy:
#
#     DATABASE_URL=${{Postgres.DATABASE_URL}}
#
# Colar uma URL a mao aqui derruba o boot com ENOTFOUND.
# Localmente (fora do Railway), use a DATABASE_PUBLIC_URL do servico Postgres:
DATABASE_URL=<COLE_AQUI_A_DATABASE_PUBLIC_URL_DO_RAILWAY>

# ---------------------------------------------------------------------------
# Autenticacao do admin
# ---------------------------------------------------------------------------
# Gere com: openssl rand -hex 32
JWT_SECRET=troque-por-uma-string-aleatoria-longa

# Criados/atualizados automaticamente no boot do servidor.
# Depois do primeiro deploy, voce pode remover ADMIN_PASSWORD das variaveis.
ADMIN_EMAIL=seu@email.com
ADMIN_PASSWORD=uma-senha-forte

# ---------------------------------------------------------------------------
# Integracoes opcionais
# ---------------------------------------------------------------------------
# Espelha os formularios numa planilha do Google. Sem isso o site funciona igual.
GOOGLE_SHEETS_WEBHOOK_URL=
